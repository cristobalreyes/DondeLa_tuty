	@model IEnumerable<DondeLa_tuty.Models.Category>

@{
    ViewBag.Title = "Store";
}

<h2>Categorias</h2>
<p>
    Seleccionar:
</p>
<ul>
    @foreach (var category in Model)
    {
        <li>@Html.ActionLink(category.Nombre,"Browse", new { category = category.Nombre})</li>
    }
</ul>

